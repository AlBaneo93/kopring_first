server :
  port : 8080
  servlet :
    context-path : /

spring :
  datasource :
    url :               jdbc:mysql://localhost:33306/kopringdb
    username :          root
    password :          root
    driver-class-name : com.mysql.cj.jdbc.Driver

  redis :
    host : localhost
    port : 36379

  jpa :
    generate-ddl :                    true
    show-sql :                        true
    hibernate :
      ddl-auto : create-drop
    defer-datasource-initialization : true

  sql:
    init :
      mode : always

jwt :
  header :
    access :  my-jwt-access
    refresh : my-jwt-refresh